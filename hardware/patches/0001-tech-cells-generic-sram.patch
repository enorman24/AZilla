diff --git a/src/rtl/tc_sram.sv b/src/rtl/tc_sram.sv
index b702a11..e1cadb6 100644
--- a/src/rtl/tc_sram.sv
+++ b/src/rtl/tc_sram.sv
@@ -62,6 +62,8 @@ module tc_sram #(
   parameter              SimInit      = "none",   // Simulation initialization
   parameter bit          PrintSimCfg  = 1'b0,     // Print configuration
   parameter              ImplKey      = "none",   // Reference to specific implementation
+  parameter bit          InitFromFile = 1'b0,     // Load init_val from a vmem file (DRAM only)
+  parameter              MemInitFile  = "",        // Default vmem path; +DRAM_INIT_FILE overrides
   // DEPENDENT PARAMETERS, DO NOT OVERWRITE!
   parameter int unsigned AddrWidth = (NumWords > 32'd1) ? $clog2(NumWords) : 32'd1,
   parameter int unsigned BeWidth   = (DataWidth + ByteWidth - 32'd1) / ByteWidth, // ceil_div
@@ -97,6 +99,20 @@ module tc_sram #(
         default:  init_val[i] = {DataWidth{1'bx}};
       endcase
     end
+    // Optional program preload (DRAM only): overwrite init_val from a vmem file so the
+    // existing `sram <= init_val` reset copy carries the image into the live array.
+    // Done at the end of this block so it wins over the SimInit fill above. Per-instance
+    // (InitFromFile) so caches/VRF never load the program. VCS/Questa path; the Verilator
+    // flow uses VerilatorMemUtil/simutil_set_mem instead and ignores this.
+    `ifndef SYNTHESIS
+    if (InitFromFile) begin
+      automatic string init_file = "";
+      if ($value$plusargs("DRAM_INIT_FILE=%s", init_file) && init_file != "")
+        $readmemh(init_file, init_val);
+      else if (MemInitFile != "")
+        $readmemh(MemInitFile, init_val);
+    end
+    `endif
   end
 
   // set the read output if requested
@@ -145,12 +161,14 @@ module tc_sram #(
         for (int unsigned i = 0; i < NumPorts; i++) begin
           if (req_i[i]) begin
             if (we_i[i]) begin
+            `ifndef VERILATOR
               // update value when write is set at clock
               for (int unsigned j = 0; j < BeWidth; j++) begin
                 if (be_i[i][j]) begin
                   sram[addr_i[i]][j*ByteWidth+:ByteWidth] <= wdata_i[i][j*ByteWidth+:ByteWidth];
                 end
               end
+            `endif
             end else begin
               // otherwise update read address for subsequent non request cycles
               r_addr_q[i] <= addr_i[i];
@@ -163,7 +181,9 @@ module tc_sram #(
     // write memory array
     always_ff @(posedge clk_i or negedge rst_ni) begin
       if (!rst_ni) begin
+        `ifndef VERILATOR
         sram <= init_val;
+        `endif
         for (int i = 0; i < NumPorts; i++) begin
           r_addr_q[i] <= {AddrWidth{1'b0}};
           // initialize the read output register for each port
@@ -186,12 +206,14 @@ module tc_sram #(
         for (int unsigned i = 0; i < NumPorts; i++) begin
           if (req_i[i]) begin
             if (we_i[i]) begin
+            `ifndef VERILATOR
               // update value when write is set at clock
               for (int unsigned j = 0; j < BeWidth; j++) begin
                 if (be_i[i][j]) begin
                   sram[addr_i[i]][j*ByteWidth+:ByteWidth] <= wdata_i[i][j*ByteWidth+:ByteWidth];
                 end
               end
+            `endif
             end else begin
               // otherwise update read address for subsequent non request cycles
               r_addr_q[i] <= addr_i[i];
@@ -202,6 +224,23 @@ module tc_sram #(
     end
   end
 
+  `ifdef VERILATOR
+  for (genvar i = 0; i < NumPorts; i++) begin
+    // update value when write is set at clock
+    for (genvar j = 0; j < DataWidth; j++) begin
+      always_ff @(posedge clk_i or negedge rst_ni) begin
+        if (!rst_ni) begin
+        end else begin
+          if (req_i[i])
+            if (we_i[i])
+              if (be_i[i][j/ByteWidth])
+                sram[addr_i[i]][j] <= wdata_i[i][j];
+        end
+      end
+    end
+  end
+  `endif
+
 // Validate parameters.
 // pragma translate_off
 `ifndef VERILATOR
@@ -242,4 +281,59 @@ module tc_sram #(
 `endif
 `endif
 // pragma translate_on
+
+  // Copyright lowRISC contributors.
+  // Licensed under the Apache License, Version 2.0, see LICENSE for details.
+  // SPDX-License-Identifier: Apache-2.0
+
+  /**
+   * Memory loader for simulation
+   *
+   * Include this file in a memory primitive to load a memory array from
+   * simulation.
+   *
+   * Requirements:
+   * - A memory array named `sram`.
+   * - A parameter `DataWidth` giving the memory width (word size) in bit.
+   * - A parameter `NumWords` giving the memory depth in words.
+   */
+
+  `ifndef SYNTHESIS
+  // Task for loading 'sram' with SystemVerilog system task $readmemh()
+  export "DPI-C" task simutil_memload;
+
+  task simutil_memload;
+    input string file;
+    $readmemh(file, sram);
+  endtask
+
+  // Function for setting a specific element in |sram|
+  // Returns 1 (true) for success, 0 (false) for errors.
+  export "DPI-C" function simutil_set_mem;
+  function int simutil_set_mem(input int index, input bit [511:0] val);
+    // Function will only work for memories <= 512 bits
+    if (DataWidth > 512)
+      return 0;
+    if (index >= NumWords)
+      return 0;
+
+    sram[index] = val[DataWidth-1:0];
+    return 1;
+  endfunction
+
+  // Function for getting a specific element in |sram|
+  export "DPI-C" function simutil_get_mem;
+  function int simutil_get_mem(input int index, output bit [511:0] val);
+    // Function will only work for memories <= 512 bits
+    if (DataWidth > 512)
+      return 0;
+    if (index >= NumWords)
+      return 0;
+
+    val                = 0;
+    val[DataWidth-1:0] = sram[index];
+    return 1;
+  endfunction
+  `endif
+
 endmodule
